<h1>List of All Songs</h1>

<style>
  table, th, td {
    border: 1px solid black;
    border-collapse: collapse;
  }
</style>

<table>
  <tr>
    <th>Song Title</th>
    <th>Artist Name</th>
    <th>Genre</th>
    <th>Release Year</th>
  </tr>

  <tbody>
    <% @songs.each do |song| %>
      <tr>
        <td><%= song.title %></td>
        <td><%= song.artist_name %></td>
        <td><%= song.genre %></td>
        <td><%= song.release_year %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br></br>
<%= link_to "Create Song", new_song_path %>
<%= link_to "Edit Song", songs_path(@song) %>
<%= link_to "Delete Song", song_path %>
